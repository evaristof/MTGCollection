# ---------------------------------------------------------------------------
# "h2" profile — H2 in-memory.
#
# Activated automatically by MtgCollectionH2Application. All the actual
# configuration lives in application.properties (which already defaults to H2);
# this file exists so the profile is explicit and so we have a place to put
# H2-only tweaks in the future.
# ---------------------------------------------------------------------------
